
const fs = require("fs");
const glob = require("glob");

// A simple script to find text nodes in TSX files and replace unescaped quotes is hard.
// Let us just let it be, or run eslint --fix if it works for this rule.

